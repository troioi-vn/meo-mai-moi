services:
  # Backend (serves both API and frontend)
  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    ports:
      - "8000:80"
    env_file:
      - ./backend/.env.docker
    networks:
      - meo-mai-moi-network
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads_data:/var/www/storage/app/public
      - logs_data:/var/www/storage/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/api/version"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Database
  db:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: meo_mai_moi
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    networks:
      - meo-mai-moi-network
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      # Only check server readiness (host:port)
      test: ["CMD", "pg_isready", "-q", "-h", "127.0.0.1", "-p", "5432", "-U", "user"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  meo-mai-moi-network:
    driver: bridge

volumes:
  pgdata:
  uploads_data:
  logs_data:

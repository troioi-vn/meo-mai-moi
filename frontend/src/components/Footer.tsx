export function Footer() {
  return (
    <footer className="w-full border-t py-4 flex justify-center items-center gap-4 bg-gray-50 text-gray-600">
      {/* Social media icons removed due to deprecation warnings. Consider replacing with Simple Icons or custom SVGs. */}
    </footer>
  )
}
